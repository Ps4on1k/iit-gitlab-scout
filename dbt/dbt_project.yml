name: 'gitlab_scout'
version: '1.0.0'
config-version: 2

profile: 'gitlab_scout'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  gitlab_scout:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: materialized_view
      +schema: marts
