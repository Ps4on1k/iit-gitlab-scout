# Plan: Compact headers + two-row filter bar

## Goal
1. Reduce header banner height by ~50% on all dashboard pages
2. Split GlobalFilterBar into two meaningful rows

## 1. Compact Header Banners

**Pattern:** Every dashboard has a gradient `<div>` with `padding: "30px 40px"`, `borderRadius: "20px"`, `marginBottom: 30`, containing `<h1 style={{ fontSize: 28, marginBottom: 10 }}>`.

**Change:** `padding: "30px 40px"` → `"14px 24px"`, `fontSize: 28` → `20`, `marginBottom: 10` → `4`, outer `marginBottom: 30` → `20`.

**Files (10 total):**
- `frontend/src/pages/Dashboard.tsx` (~line 110)
- `frontend/src/components/contributors/ContributorDashboard.tsx` (~line 189)
- `frontend/src/components/contributors/DeployReliabilityDashboard.tsx` (~line 39)
- `frontend/src/components/activity/ActivityDashboard.tsx` (~line 108)
- `frontend/src/components/branches/BranchDashboard.tsx` (~line 160)
- `frontend/src/components/pipelines/PipelineDashboard.tsx` (~line 68)
- `frontend/src/components/stack/StackDashboard.tsx` (~line 94)
- `frontend/src/components/dora/DoraDashboard.tsx` (~line 109)
- `frontend/src/components/benchmark/BenchmarkDashboard.tsx` (~line 121)
- `frontend/src/components/analytics/AnalyticsDashboard.tsx` (~line 60)

## 2. Two-Row GlobalFilterBar

**File:** `frontend/src/components/GlobalFilterBar.tsx`

**Current:** One row with 8 controls crammed together (RangePicker, 3 Selects, Reset, Mean/Median switch, Presets, Share).

**New layout:**
- **Row 1 (filters):** RangePicker + Теги + Проекты + Контрибьюторы (flex: 1 each)
- **Row 2 (actions):** Reset, Mean/Median, Presets, Share — right-aligned with `justifyContent: "flex-end"`
- **Row 3 (active tags, conditional):** existing tag chips row, unchanged

## Verification
- `docker compose up -d --build`
- Open each tab (Обзор, Коммиты, Надёжность деплоя, Активность, Ветки, CI/CD, DORA, Языки, Бенчмарк) — headers should be compact
- GlobalFilterBar should show two rows: filters on top, action buttons below
- All filters should still work, tags row appears when filters are active
