# Plan: Task duration tracking + Branches layout + Deploy Reliability tab

## Task 1: Duration tracking for each collection category

**What:** Show how long each task took on the last run, displayed below "Последний запуск" with minimal spacing.

**Backend changes:**
- `backend/src/services/scheduler.ts` — track `taskStartedAt` before each `runTask()`, compute duration after, store via new helper
- `backend/src/api/v1/scheduler.ts` — add `last_duration` to status endpoint response (from in-memory map keyed by task_name)

**Frontend changes:**
- `frontend/src/components/SchedulerPanel.tsx` — update `last_run_at` column render to show duration on second line with `lineHeight: 1.2`

## Task 2: Branches table — project name + tags wrapping

**File:** `frontend/src/components/common/ProjectLabel.tsx`
- Already updated in previous plan (div with flex-wrap, separate Tag components)

## Task 3: Restore "Надёжность деплоя" tab

**File:** `frontend/src/App.tsx`
- Add `"deploy-reliability"` to AnalyticsTab type
- Add tab label after "Контрибьюторы"
- Add lazy import for DeployReliabilityDashboard
- Add rendering in Suspense block

## Verification
- `npx tsc --noEmit` in frontend
- Visual check: scheduler table shows duration below last_run_at
- Visual check: "Надёжность" tab appears and loads
- Visual check: branches table shows project name + tags on separate wrapping lines
