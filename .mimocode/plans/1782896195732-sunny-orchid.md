# Plan: Configurable version check URLs + Dependency fixes

## 1. Add `version_check_url` to dependency_catalog

**Migration** `042_add_version_check_url.cjs`:
```sql
ALTER TABLE dependency_catalog ADD COLUMN version_check_url text;
```

**Seed default URLs per ecosystem:**
- npm: `https://registry.npmjs.org/{name}/latest`
- pip: `https://pypi.org/pypi/{name}/json`
- nuget: `https://api.nuget.org/v3-flatcontainer/{name}/index.json`
- go: `https://proxy.golang.org/{name}/@latest`
- maven: `https://search.maven.org/solrsearch/select?q=g:{group}+AND+a:{artifact}&rows=1&wt=json`

## 2. Update dependency-audit.ts

- Load `version_check_url` from catalog
- Pass URL template to `checkLatestVersion`
- Add `{name}` placeholder replacement

## 3. Update DependencyCatalogPanel

- Show `version_check_url` column
- Make it editable per ecosystem

## 4. Fix dependency collection issues

- **npm**: Add `peerDependencies` parsing
- **iOS**: Add `Podfile.lock` parsing
- **Android**: Add `libs.versions.toml` parsing

## 5. Fix outdated check

- Add logging for failed checks
- Normalize version prefixes (v1.2.3 → 1.2.3)

## Files to modify
1. `backend/migrations/042_add_version_check_url.cjs` - create
2. `backend/src/services/dependency-audit.ts` - use catalog URLs
3. `frontend/src/components/DependenciesCatalogPanel.tsx` - show/edit URL
